services:
    frontend-service:
        build:
            context: ./student1/frontend-service
        container_name: frontend-service
        ports:
            - "8080:80"
        depends_on:
            - enrolment-service
        networks:
            - enrolment-network
        restart: unless-stopped

    enrolment-service:
        build:
            context: .
            dockerfile: student1/enrolment-service/Dockerfile
        container_name: enrolment-service
        ports:
            - "5001:5001"
        environment:
            DATABASE_SERVICE_URL: http://database-service:5002
            OLLAMA_BASE_URL: http://host.docker.internal:11434/v1
            OLLAMA_MODEL: qwen2.5:0.5b
        extra_hosts:
            - "host.docker.internal:host-gateway"
        depends_on:
            - database-service
        networks:
            - enrolment-network
        restart: unless-stopped

    database-service:
        build:
            context: ./student1/database-service
        container_name: database-service
        ports:
            - "5002:5002"
        volumes:
            - database_data:/app/data
        networks:
            enrolment-network:
                aliases:
                    - database-service
        restart: unless-stopped

volumes:
    database_data:

networks:
    enrolment-network:
        driver: bridge